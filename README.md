# Food Vision CNN

Jupyter notebook: `food_vision_cnn.ipynb`

Description:
- CNN for food classification / vision tasks (dataset and details in the notebook).

How to run:
1. Open the notebook in Jupyter, JupyterLab, or Google Colab.
2. Install dependencies (example):

```
pip install tensorflow numpy matplotlib pillow
```

Notes:
- Use `scripts/package_repos.py` to prepare this folder as a separate repository and push to GitHub when ready.
